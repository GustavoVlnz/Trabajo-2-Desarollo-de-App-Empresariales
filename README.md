## Requisitos para ejecutar el programa

- Python 3.10 o superior
- Biblioteca externa:
  - `customtkinter`

## Instalación de dependencias

Ejecuta el siguiente comando en tu terminal:

```powershell
pip install customtkinter
```

> Si tiene varias versiones de Python instaladas, puede que necesite usar `python -m pip install customtkinter` o `py -m pip install customtkinter`.

## Notas

- El programa utiliza `customtkinter` para la interfaz gráfica.
- Las demás bibliotecas (`csv`, `os`, `random`, `re`) pertenecen a la biblioteca estándar de Python y no requieren instalación adicional.
